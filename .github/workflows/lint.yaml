name: Linter
on:
  push:
    branches: [main, develop]
    paths:
      - '**/*.go'
      - 'go.mod'
      - 'go.sum'
      - '.golangci.yml'
      - '.golangci.yaml'
  pull_request:
    branches: [main, develop]
    paths:
      - '**/*.go'
      - 'go.mod'
      - 'go.sum'
      - '.golangci.yml'
      - '.golangci.yaml'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    env:
      # private modules (apply to all steps)
      GOPRIVATE: github.com/bitmark-inc/*,github.com/feral-file/*
      GONOSUMDB: github.com/bitmark-inc/*,github.com/feral-file/*
      # Optional: bypass proxy for private
      GOPROXY: https://proxy.golang.org,direct
      # Optional: avoid CGO needs during analysis
      CGO_ENABLED: 0
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup git config for private repo
        env:
          GITHUB_TOKEN: ${{ secrets.ACTION_GITHUB_TOKEN }}
        run: |
          git config --global "url.https://${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
          cache-dependency-path: |
            **/go.sum

      - name: Install dependencies
        run: go mod download

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.4.0
          args: ./... --timeout=5m --verbose
