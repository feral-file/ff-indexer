apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nft-event-processor-tezos-emitter.fullname" . }}-test-connection"
  labels:
    {{- include "nft-event-processor-tezos-emitter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nft-event-processor-tezos-emitter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
