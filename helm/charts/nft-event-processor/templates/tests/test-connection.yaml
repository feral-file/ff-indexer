apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nft-event-processor.fullname" . }}-test-connection"
  labels:
    {{- include "nft-event-processor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nft-event-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
