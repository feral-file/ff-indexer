apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nft-indexer.fullname" . }}-test-connection"
  labels:
    {{- include "nft-indexer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nft-indexer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
