apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nft-event-subscriber.fullname" . }}-test-connection"
  labels:
    {{- include "nft-event-subscriber.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nft-event-subscriber.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
