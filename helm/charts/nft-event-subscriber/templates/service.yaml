apiVersion: v1
kind: Service
metadata:
  name: {{ include "nft-event-subscriber.fullname" . }}
  labels:
    {{- include "nft-event-subscriber.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nft-event-subscriber.selectorLabels" . | nindent 4 }}
